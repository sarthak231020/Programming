#include<iostream>
#include<vector>
using namespace std;

int main()
{
    int T,N,K;
    string S;   
    cin>>T;
    while(T--)
    {
        cin>>N>>K;
        cin>>S;
        vector<string> V;
        for(auto i:S)
        {
            V.emplace_back(i);
        }
        while(V.size() > K)
        {
            V = 
        }
    }
}